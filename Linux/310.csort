{
}
	{
	{
	}
	}
		{
		}
#define SIZE 5
	else if (pid > 0)
		for (i = 0; i < SIZE; ++i)
		for (i = 0; i < SIZE; ++i)
	if (pid == 0)
#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>
	int i;
int main(void)
int nums[SIZE] = {0, 1, 2, 3, 4};
			nums[i] *= -i;
	pid = fork();
	pid_t pid;
			printf("child: %d", nums[i]);
			printf("parent: %d", nums[i]);
	return 0;
		wait(NULL);
